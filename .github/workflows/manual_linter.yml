name: 🛠️ 手动触发 - Mod 完整性检测

# workflow_dispatch 是实现"仅手动运行"的核心
on:
  workflow_dispatch:
    inputs:
      log_level:
        description: '检测严格程度 (输出日志级别)'
        required: true
        default: 'WARNING'
        type: choice
        options:
          - INFO
          - WARNING
          - ERROR
          - DEBUG

jobs:
  mod-linter:
    runs-on: ubuntu-latest

    steps:
      # 1. 检出当前 Mod 的代码
      - name: 📦 检出 Mod 源码
        uses: actions/checkout@v4

      # 2. 检出检测工具代码 (从 stellaris_cwt_linter 仓库)
      - name: 📥 检出检测工具代码
        uses: actions/checkout@v4
        with:
          repository: cocolinfff/stellaris_cwt_linter
          path: ./linter_tool
          ref: master

      # 3. 配置 Python 环境
      - name: 🐍 设置 Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 4. 运行检测工具并捕获输出
      - name: 🚀 运行完整性检测
        id: run_linter
        run: |
          echo "开始运行检测工具..."
          echo "当前目录: $(pwd)"
          # 当前目录就是 Mod 目录，工具在 ./linter_tool 子目录
          # 运行工具，传入当前目录 (.) 作为 mod 路径
          # 使用子shell确保工作目录自动恢复
          if [ -d linter_tool ]; then
            (cd linter_tool && python main.py .. > ../linter_report.txt 2>&1) || true
          else
            echo "错误: linter_tool 目录不存在"
            echo "LINTER_FAILED=true" >> $GITHUB_ENV
            exit 1
          fi
          summary_report="linter_report.txt"
          if [ -f linter_report.txt ]; then
            echo "报告已生成"
            ls -la linter_report.txt
            echo "报告内容前10行:"
            head -10 linter_report.txt
            # 检查工具是否崩溃 (检测 Python Traceback 标识)
            if grep -q "^Traceback" linter_report.txt; then
              echo "LINTER_FAILED=true" >> $GITHUB_ENV
            fi
          else
            echo "报告文件生成失败"
            echo "LINTER_FAILED=true" >> $GITHUB_ENV
          fi
          # Extract the full analysis report file
          latest_report=$(find linter_tool -maxdepth 1 -type f -name 'mod_analysis_*' -printf '%T@ %p\n' | sort -rn | head -n1 | cut -d' ' -f2-)
          if [ -n "$latest_report" ]; then
            report_name=$(basename "$latest_report")
            cp "$latest_report" "./$report_name"
            summary_report="$report_name"
            echo "Found analysis report file: $report_name"
          else
            echo "mod_analysis report file not found"
          fi
          echo "SUMMARY_REPORT=$summary_report" >> $GITHUB_ENV

      # 5. 输出完整报告内容
      - name: 📄 输出完整报告
        run: |
          if [ -f "$SUMMARY_REPORT" ]; then
            echo "## 📄 完整检测报告" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```text' >> $GITHUB_STEP_SUMMARY
            cat "$SUMMARY_REPORT" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          else
            echo "报告文件生成失败" >> $GITHUB_STEP_SUMMARY
          fi

      # 6. 提供完整日志文件供下载
      - name: 💾 上传完整日志附件
        uses: actions/upload-artifact@v4
        with:
          name: Mod-Linter-Report-${{ github.ref_name }}
          path: linter_report.txt
          retention-days: 7

      - name: 💾 上传分析报告附件
        if: env.SUMMARY_REPORT != '' && env.SUMMARY_REPORT != 'linter_report.txt'
        uses: actions/upload-artifact@v4
        with:
          name: Mod-Analysis-Report-${{ github.ref_name }}
          path: ${{ env.SUMMARY_REPORT }}
          retention-days: 7

      # 7. 如果上一步检测出致命错误，在这里让 Action 标红变败
      - name: 🛑 判定最终状态
        if: env.LINTER_FAILED == 'true'
        run: |
          echo "::error::检测到致命错误或断链，请查看上方报告！"
          exit 0
