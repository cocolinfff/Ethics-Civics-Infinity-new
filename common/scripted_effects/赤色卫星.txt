red_origin_fiefdom_jhh_specialization_effect = {
    custom_tooltip = red_origin.5705.g.tt

    hidden_effect = {
        random_agreement = {
            limit = {
                owner = { is_same_value = root.overlord }
                target = { is_same_value = root }
            }
            set_agreement_preset = { preset = preset_jhh_subject }
        }

    hidden_effect = {
        create_fleet = {
            effect = {
                set_owner = root
                create_ship = {
                    name = random
                    random_existing_design = science
                }
                set_location = root.capital_star
                save_event_target_as = new_science_ship_target
            }
        }
        create_leader = {
            name = random
            species = root.overlord.species
            class = scientist
            skill = 3
        }
        event_target:new_science_ship_target = {
            assign_leader = last_created_leader
        }

        create_fleet = {
            effect = {
                set_owner = root
                create_ship = {
                    name = random
                    random_existing_design = constructor
                }
                set_location = root.capital_star
            }
        }
    }
}