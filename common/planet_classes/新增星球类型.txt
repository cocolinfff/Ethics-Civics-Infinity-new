pc_wandering_planet = {
    entity = "continental_planet"  # Ignored for arkships; the actual ship model is used instead
    icon = GFX_planet_type_generic_arkship
    icon_large = GFX_planet_type_generic_arkship_big
    conditional_icon = {
        trigger = { always = yes }
        icon = GFX_planet_type_machine
        icon_large = GFX_planet_type_machine_big
    }
    modifier = {
    }
    climate = "artificial"
    initial = yes
    starting_planet = no
    starting_district = district_city
    district_set = standard
    # starting_district = district_ark_city
    # district_set = nomad
    atmosphere_color		= hsv { 0.59 0.45 0.95 }
    atmosphere_intensity	= 1.0
    atmosphere_width		= 0.5
    min_distance_from_sun = @habitable_min_distance
    max_distance_from_sun = @habitable_max_distance
    spawn_odds = 0
    extra_orbit_size = 0
    extra_planet_count = 0
    planet_size = { min = 10 max = 10 }
    colonizable = yes
    uses_alternative_skies_for_moons = no
    uses_alternative_skies_if_has_orbital_ring = no
    is_artificial_planet = yes
    default_planet_selection = yes
    carry_cap_per_free_district = @carry_cap_normal
    single_build_queue = yes
}
