@contribution_low = 0.1
@cooldown = 3600

MSI_law_unified_federal_currency_none = {
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_none }
					has_modifier = federation_MSI_law_unified_federal_currency_type_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_type
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_type_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0
		modifier = {
			desc = federation_acceptance_xenophobic_isolationists
			add = 10
			has_ai_personality = xenophobic_isolationists
		}
		modifier = {
			desc = federation_acceptance_xenophobe
			add = 10
			is_xenophobe = yes
		}
	}
}

MSI_law_unified_federal_currency_type_base = {
	allow = {
		custom_tooltip = {
			fail_text = "requires_federation_lvl_3"
			federation_level > 2
		}
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_type_base }
					has_modifier = federation_MSI_law_unified_federal_currency_type_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_type
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_type_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

MSI_law_unified_federal_currency_type_advanced = {
	allow = {
		custom_tooltip = {
			fail_text = "requires_federation_lvl_3"
			federation_level > 2
		}
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_type_advanced }
					has_modifier = federation_MSI_law_unified_federal_currency_type_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_type
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_type_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

MSI_law_unified_federal_currency_type_special = {
	allow = {
		custom_tooltip = {
			fail_text = "requires_federation_lvl_3"
			federation_level > 2
		}
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_type_special }
					has_modifier = federation_MSI_law_unified_federal_currency_type_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_type
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_type_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

########################################################################

MSI_law_unified_federal_currency_energy = {
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_energy }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_base
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

MSI_law_unified_federal_currency_minerals = {
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_minerals }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_base
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

MSI_law_unified_federal_currency_food = {
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_food }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_base
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

########################################################################

MSI_law_unified_federal_currency_consumer_goods = {
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_consumer_goods }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_advanced
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

MSI_law_unified_federal_currency_alloys = {
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_alloys }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_advanced
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

MSI_law_unified_federal_currency_research = {
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_research }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_advanced
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

########################################################################

MSI_law_unified_federal_currency_unity = {
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_unity }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_special
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

MSI_law_unified_federal_currency_trade = {
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_trade }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_special
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

MSI_law_unified_federal_currency_sr_zro = {
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_sr_zro }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_special
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

MSI_law_unified_federal_currency_sr_dark_matter = {
	potential = {
		federation_leader = {
			has_valid_civic = civic_dark_consortium
			NOT = { has_origin = origin_wizarding_world }
		}
	}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_sr_dark_matter }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_special
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

MSI_law_unified_federal_currency_astral_threads = {
	potential = {
		federation_leader = {
			NOT = { has_valid_civic = civic_dark_consortium }
			has_origin = origin_wizarding_world
		}
	}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_astral_threads }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_special
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}

MSI_law_unified_federal_currency_sr_dark_matter_astral_threads = {
	potential = {
		federation_leader = {
			has_valid_civic = civic_dark_consortium
			has_origin = origin_wizarding_world
		}
	}
	allow = {
		custom_tooltip = {
			NOT = {
				AND = {
					NOT = { has_federation_law = MSI_law_unified_federal_currency_sr_dark_matter_astral_threads }
					has_modifier = federation_MSI_law_unified_federal_currency_cooldown
					has_any_federation_law_in_category = MSI_law_unified_federal_currency_special
				}
			}
		}
	}
	cohesion_growth = 0.25
	on_enact = {
		if = {
			limit = { has_federation_flag = enable_federation_cooldowns }
			add_modifier = {
				modifier = federation_MSI_law_unified_federal_currency_cooldown
				days = @cooldown
			}
		}
		add_cohesion = -5
		hidden_effect = {
			if = {
				limit = {
					has_federation_flag = no_federation_fleet
				}
				remove_federation_flag = no_federation_fleet
				set_timed_federation_flag = {
					flag = no_federation_fleet_build_right_cooldown
					days = 10
				}
			}
		}
	}

	ai_weight = {
		base = 0

		#Default support
		modifier = {
			desc = federation_acceptance_generally_positive
			add = 20
		}

		modifier = {
			add = 10
			desc = federation_acceptance_high_cohesion
			federation = {
				federation_cohesion >= 99
				has_federation_law = fleet_contribution_none
			}
		}
	}
}