namespace = ethic_administration

country_event = { # 初始化行政机制（开局触发）
    id = ethic_administration.0
    is_triggered_only = yes
    hide_window = yes
    trigger = { # AI不发生，节约算力并保护AI
        OR = {
			is_ai = no
			has_global_flag = rea_ai
		}
    }
    immediate = {
		if = {
			limit = {
				always = yes
			}
			start_situation = { type = situation_administration }
		}
    }
}

situation_event = { # 行政体系更新（国家月检）
	id = ethic_administration.1
    is_triggered_only = yes
    hide_window = yes
    immediate = {
		rea_update_monthly = yes
    }
}

situation_event = { # 行政情况报告
	id = ethic_administration.9
    is_triggered_only = yes
	desc = ethic_administration.9.desc
}

situation_event = { # DEBUG
	id = ethic_administration.99999
    is_triggered_only = yes
	desc = ethic_administration.99999.desc
}